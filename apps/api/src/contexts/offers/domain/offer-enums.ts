import { NeedCategory } from '../../needs/domain/need-enums';

export { NeedCategory };

export enum OfferStatus {
  Open = 'open',
  Matched = 'matched',
  Fulfilled = 'fulfilled',
  Cancelled = 'cancelled',
}
