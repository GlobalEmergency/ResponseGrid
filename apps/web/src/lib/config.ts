/**
 * Demo emergency ID used while the public self-registration screen is not yet
 * scoped to a specific emergency via a URL slug.
 *
 * In production the emergencyId will come from the active-emergency context
 * (e.g. a URL param like /emergencias/[slug]/registrar).
 */
export const DEMO_EMERGENCY_ID =
  '11111111-1111-4111-8111-111111111111';
